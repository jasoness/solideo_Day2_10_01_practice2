# 🚀 여행 개인화 앱 실행 가이드

## 빠른 시작

### 1. 의존성 설치 (최초 1회만)
```bash
npm install
```

### 2. 개발 서버 시작
```bash
npm run dev
```

### 3. 브라우저에서 접속
주소: **http://localhost:5173/**

### 4. 서버 중지
터미널에서 `Ctrl + C` (Windows/Mac/Linux 공통)

---

## 📋 상세 실행 가이드

### 필수 요구사항
- Node.js 16 이상
- npm 또는 yarn
- 최신 브라우저 (Chrome, Firefox, Edge, Safari)

### 설치 단계

#### 1단계: Node.js 설치 확인
```bash
node --version
npm --version
```

출력 예시:
```
v22.21.0
10.9.4
```

Node.js가 설치되어 있지 않다면:
- 공식 사이트: https://nodejs.org/
- LTS 버전 다운로드 및 설치

#### 2단계: 프로젝트 디렉토리로 이동
```bash
cd solideo_Day2_10_01_practice2
```

#### 3단계: 의존성 설치
```bash
npm install
```

이 명령어는:
- `node_modules` 폴더 생성
- 필요한 모든 라이브러리 다운로드 (React, Vite 등)
- 약 1-2분 소요

#### 4단계: 개발 서버 시작
```bash
npm run dev
```

성공 메시지:
```
  VITE v5.4.21  ready in 277 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

#### 5단계: 브라우저 접속
웹 브라우저에서 다음 주소 입력:
```
http://localhost:5173/
```

또는 간단히:
```
localhost:5173
```

---

## 🎯 앱 사용법

### 화면 1: 여행 정보 입력

| 항목 | 설명 | 예시 |
|------|------|------|
| 출발 건물/역 | 출발 위치 입력 | 서울역, 강남역, 인천공항 |
| 도착 건물/역 | 목적지 입력 | 부산역, 제주공항, 경주역 |
| 출발 시간 | 날짜와 시간 선택 | 2025-11-10 09:00 |
| 여행 기간 | 일 단위 (1-30일) | 2일 |

**"검색하기"** 버튼을 클릭하면 다음 단계로 이동합니다.

### 화면 2: 취향 선택

#### 관심사 (복수 선택 가능)
- 자연
- 해변
- 문화
- 역사
- 쇼핑
- 야경
- 가족
- 로맨틱

#### 음식 취향 (복수 선택 가능)
- 한식
- 중식
- 일식
- 양식
- 해산물
- 면요리
- 현지음식
- 디저트

#### 예산 (하나 선택)
- 💰 저렴
- 💰💰 보통
- 💰💰💰 고급

**"추천 받기"** 버튼을 클릭하면 결과 화면으로 이동합니다.

### 화면 3: 결과 확인

#### 왼쪽 패널

**1. 대중교통 검색**
- 필터: 전체 / 버스 / 기차 / 비행기
- 각 옵션 표시 정보:
  - 교통수단 아이콘 (🚌🚄✈️)
  - 운영 회사
  - 출발/도착 시간
  - 소요 시간
  - 가격
- 카드 클릭으로 선택

**2. 맞춤 추천**
- 필터: 전체 / 관광지 / 맛집
- 각 추천 표시 정보:
  - 장소 이름
  - 평점 (⭐)
  - 설명
  - 태그 (#자연 #해변 등)
- 카드 클릭으로 경로에 추가

#### 오른쪽 패널

**1. 구글 맵 시각화**
- 출발지 📍
- 도착지 🎯
- 선택한 경유지 📌
- 경로선 표시

**2. 선택한 장소**
- 추가된 장소 목록
- "제거" 버튼으로 삭제 가능

---

## 🔧 추가 설정

### 지도 API 키 - 필요 없음! 🎉

이 앱은 **OpenStreetMap**과 **Leaflet**을 사용하므로:
- ✅ API 키가 전혀 필요하지 않습니다!
- ✅ 무료로 사용 가능합니다!
- ✅ 사용량 제한이 없습니다!
- ✅ 회원가입이나 설정 없이 바로 실행됩니다!

지도 기능은 앱을 실행하면 즉시 작동합니다. 별도의 설정이 필요 없습니다.

---

## 🏗️ 프로덕션 빌드

실제 배포용 빌드를 만들려면:

```bash
npm run build
```

빌드된 파일은 `dist` 폴더에 생성됩니다.

빌드 미리보기:
```bash
npm run preview
```

---

## 🐛 문제 해결

### 포트 5173이 이미 사용 중
```
Error: Port 5173 is already in use
```

**해결 방법**:
1. 다른 터미널에서 실행 중인 서버 종료
2. 또는 다른 포트 사용:
```bash
npm run dev -- --port 3000
```

### node_modules 오류
```
Error: Cannot find module
```

**해결 방법**:
```bash
rm -rf node_modules
npm install
```

### 브라우저에서 연결 실패
```
This site can't be reached
```

**확인 사항**:
1. 개발 서버가 실행 중인지 확인
2. 터미널에서 "ready in" 메시지 확인
3. http:// 프로토콜 확인 (https가 아님)

---

## 📞 지원

문제가 발생하면:
1. README.md 파일 참조
2. 터미널의 오류 메시지 확인
3. 브라우저 개발자 도구 (F12) 콘솔 확인

---

**즐거운 여행 계획 되세요! ✈️**
